# Build output
/civitai-manager
/dist/

# Nix build symlinks
/result
/result-*

# Local runtime data
*.db
*.db-wal
*.db-shm

# Editor / OS cruft
.DS_Store
*.swp

# Scratch
/scratch/

# Claude Code agent worktrees / local agent state. These land INSIDE the repo,
# so leaving them untracked-but-unignored sweeps them into `src = ./.` style Nix
# builds and dirties `git status`.
/.claude/
