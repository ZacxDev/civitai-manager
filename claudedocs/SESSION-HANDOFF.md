# civitai-manager — session handoff

_Point-in-time snapshot. Verify against `git log`/live code before acting._

## Current state
- **Latest release: v0.1.33** (`main` @ `e84bbc4`). 15 releases this session (v0.1.19 → v0.1.33).
- **ComfyUI integration IN FLIGHT** — see `claudedocs/COMFYUI-INTEGRATION-DESIGN.md`. **Slice A (Workflow Library) → v0.1.28; Slice A2 (scanning + Library tab + auto-link) → v0.1.29; Slice B (local ComfyUI run + UI→API converter) → v0.1.30.** Next: **Slice C** (remote CivitAI Comfy Cloud) — see Open threads.
- **A local ComfyUI (v0.27.1) runs at `http://127.0.0.1:8188`** — the default `comfy_url`. The `civitai` CLI at `/home/zach/go/bin/civitai` can pull real workflows (`models search --type Workflows` → `download` → zips of `.json`) + `images search --meta`; used for real-workflow verification.
- Local `main` is clean. **`export GOPRIVATE=github.com/civitai/*`** before any `go build`/`vet`/`test` (private `github.com/civitai/cli` SDK dep) — otherwise the build fails on sumdb verification. See repo `CLAUDE.md` (read it first).
- **Standing release authorization:** the user granted (2026-07-24) blanket OK to push+tag+release WITHOUT per-release approval — still run the full gate + `/audit-pr` + live-verify first, then ship and report. (Recorded in auto-memory `civitai-manager-release-authorization`.)

## What shipped v0.1.20 → v0.1.27
- **v0.1.20** — results-view render caps (matched 200 / unmatched 500 / candidates 500) + sort-before-cap (biggest rendered) + cap-immune "Quarantine all &lt;reason&gt;" buttons.
- **v0.1.21** — scan-progress focus (scan form hidden while a scan runs; lives inside `#scan-results`); dashboard/search overhaul: search-card image carousels (manager-side raw-JSON parse), "Popular this month" default (TTL cache), integrated `/subscribe/search`, library-derived subscribe suggestions.
- **v0.1.22** — downscaled civitai image thumbnails for showcase tiles (`civitaiThumbURL`, host-gated to image.civitai.com; lightbox keeps original).
- **v0.1.23** — keyword search sorts Most Downloaded; **lazy community-image feed** on the model page (`GET /models/{id}/community`, `SearchImages` by version, `hx-trigger=revealed`, 15s bounded); showcase carousel moved into the model header; description overflow constrained (`.cm-model-desc`).
- **v0.1.24** — Library defaults to Model files tab when set up; author `@link` on matched cards; **version breakdown** (in-library / available / newer + links) + subscribe toggle; **global NSFW control in navbar** (3-state, HX-Refresh; per-page control removed); model-page version badges.
- **v0.1.25** — perf pass: indexed per-model lookups (`ListLocalFilesByModel` + `FindModelSubscription`) replacing full-table scans on the Model files tab.
- **v0.1.26** — subscribe UX: inline **options + confirm + feedback** control (`/models/{id}/subscribe-options|subscribe-control`); suggestion titles cache-first + lazy (`/models/{id}/title`); **search NSFW-param fix** (images were withheld — `nsfw=true` under blur/show, `false` under hide; popular cache re-keyed per flag); compact K/M counts; audit-fix: subscribe FAILURE now shows feedback.
- **v0.1.27** — subscribe control reflects real subscribed/not state on the model page + search/creator cards (one `ListSubscriptions` per render; no per-card scan).
- **v0.1.28** — **ComfyUI Slice A: Workflow Library** (pure-Go, no execution). New `workflows` table (migration `0008`, `is_golden` singleton per version via partial unique index) + store CRUD (`internal/store/workflows.go`); new **`internal/comfy`** package — untrusted PNG `tEXt` metadata extraction (`prompt`→API-format graph, `workflow`→UI-format; `parameters`→A1111 rejected) + api/ui format detection + loader-node resource extraction, all bounded/defensive (64 MiB cap before alloc); new **Workflows nav tab** (`internal/web/workflow_{handlers,pages}.go`) — import via paste-JSON or PNG upload, list/detail/attach/set-golden, CSRF + loopback-gated imports, multipart temp-file cleanup. Run button greyed (execution = Slice B). Audit verdict safe-to-merge; the one 🟡 (multipart temp-file leak, leaked pre-CSRF) fixed + empirically re-verified.
- **v0.1.29** — **ComfyUI Slice A2: workflow scanning + Library tab + auto-link.** Workflows **relocated from a top-level nav item into a tab on the Library page** (`/library?tab=workflows`; `GET /workflows` 303→ the tab). Migration `0009` adds `source_path`(partial-unique)/`size_bytes`/`mtime` for a path-keyed re-scan cache. Install-discovery (`detectComfyUI`) now surfaces `user/default/workflows` + `output` dirs (`Install.WorkflowDirs`/`OutputDir`, `WorkflowScanDirs`). New **workflow scanner** (`internal/library/workflow_scan.go`) walks those dirs for `.json`, parses (api+ui), extracts resources (**UI-aware** `widgets_values` heuristic), and **auto-links** to the local model version by unambiguous basename match. Race-safe streaming **workflow-scan job** (`internal/web/workflow_scan_handlers.go`, `POST /library/workflow-scan` + status/stop, CSRF + loopback-gated) with a **Scan workflows** button in the tab. Audit no-🔴; both 🟡 fixed: (1) an unchanged-but-**unlinked** workflow now **re-links** on re-scan once its model is installed (cache-hit no longer freezes it), (2) **ambiguous** basename (same filename across ≥2 models) is left unlinked instead of guessing. Live-verified against a real ComfyUI install layout + seeded temp DB (2 found/2 linked/1 skipped; re-link repro; no dup rows). **Caveat:** most ComfyUI *saved* workflows are UI-format → resource-extraction/auto-link work, but **running** them still needs Slice B's UI→API converter.
- **v0.1.30** — **ComfyUI Slice B: local run + UI→API converter.** Run a workflow on the local ComfyUI from the Workflows tab (Run button enabled). New `internal/comfy/client.go` — **poll-based** HTTP client (no WS dep): `Submit`/`ObjectInfo`/`History`/`QueueState`/`View`/`Interrupt`/`SystemStats`, separate loopback-allowed `*http.Client` (redirect-guarded), optional bearer. `convert.go` — **UI→API `graphToPrompt`** (widget-order mapping via `/object_info`, link resolution → `[id,slot]`, **seed/`control_after_generate` off-by-one**, Reroute splice, bypass/unknown-node → warnings). `preflight.go` — missing nodes/models vs `/object_info` + local library (`store.HasLocalFileNamed`). Config `comfy_url` (default `http://127.0.0.1:8188`) + secret `comfy_token`. Race-safe run job (`internal/web/run_handlers.go`) + result gallery via a **prompt-scoped, image-only, nosniff** `/workflows/run/view` proxy. Audit no-🔴 (3 🟡 hardened: client redirect guard, view content-type, run-goroutine `recover`). **`NewID()` UUID fix** — ComfyUI 0.27 rejects a non-UUID `prompt_id`; caught by live-verify, would've broken every real run. **Live-verified end-to-end vs the real ComfyUI**: a real civitai workflow converted UI→API, submitted via the product run endpoint, ran to completion, returned a real 1216×832 PNG; proxy rejects spoofed prompt-id + traversal (403). **Caveats:** run aborts on ANY conversion warning (so a workflow using an uninstalled custom node won't submit until installed); one global active run (not per-workflow); poll-based progress (coarser than WS); converter edge cases not exercised live — object-form `widgets_values`, complex bypass chains, group nodes, primitive-node promotion (see the merge commit / Slice B agent report).
- **v0.1.31** — **workflow-scan dir-resolution fix** (`resolveWorkflowScanDirs`). The scan was running a synchronous `$HOME` discovery crawl first (6s deadline, non-deterministic, ~29s scans) and walking each marked install ROOT recursively — sweeping in bundled custom-node examples + ComfyUI templates (real install: 226 noise vs 62 saved) and sometimes appearing to find nothing. Now: when install dirs are marked, derive each one's `user/default/workflows` (`library.WorkflowDirsForMarked`, reuses `comfyDerivedDirs`) and scan only that; the `$HOME` crawl is a fallback used ONLY when nothing is marked. Live-verified on the real install: **2s (was 29s), 61 user workflows (was 288), 42 auto-linked, 0 noise**. Small focused fix — full gate + targeted tests + real live-verify; did NOT run a separate `/audit-pr` subagent (dir-resolution only, no new endpoint/migration/untrusted-surface).
- **v0.1.32** — **workflow-UI polish** (4 features): (1) import forms moved into a native `<dialog>` modal behind an "Import a workflow" button; (2) list resolves **model + version names** (local `model_cache` first, lazy `/models/{id}/title` when uncached; `versionNameFromRaw` parses the cached model raw), linked to `/models/{id}`, and "N resources" expands (`<details>`) into per-file **have✓/missing✗** badges (`HasLocalFileNamed`); (3) detail page renders the workflow as a **server-side SVG** from its saved `pos`/`size`/`links` (`internal/web/workflow_graph.go`, litegraph coords + type-colored wires; structured fallback for api-format; raw JSON behind a disclosure); (4) Run gated behind a **ComfyUI reachability pill** (`GET /workflows/{id}/run/comfy-status`, own 3s ping timeout, green→enabled / red→disabled+Recheck). No reusable OSS server-side comfy-graph renderer exists (litegraph/Workflow-Inspector are browser/Canvas) → built our own per the [litegraph spec](https://docs.comfy.org/specs/workflow_json). Audit no-🔴, **SVG XSS verified clean** (all untrusted text via `g.Text`, attrs are coerced numbers); the one 🟡 (**unbounded render** → DoS) fixed: cap 600 nodes / 2000 links + "showing first N of M" note + paste-import `MaxBytesReader`. Live-verified vs real ComfyUI + 61 real workflows: modal, resolved titles, 187 have✓/2 missing✗, a real 32-node SVG, reachable→enabled Run, and a **crafted `</text><script>` node-title/widget rendered escaped** (0 script exec, 0 attr injection). **Caveat:** SVG *visual* layout unverified (no browser) — a single fixed palette (not `data-theme`-token-driven), and node/label overlap on dense graphs is unchecked; uncached version names show `version {id}`.
- **v0.1.33** — **library/dashboard/search UI batch** (4 features): (1) **search sort** — dashboard mini-search now sends `sort=Most Downloaded`; search page gains a whitelisted **sort+period dropdown** (`normalizeSearchSort`/`Period`, both keyword+default paths); (2) library action bar redesigned into **color-coded stat pills** (📦 models · ⧉ dupes+reclaim · ⟳ out-of-date · ⚠ broken · ○ unmatched) + a **cache-only "out of date" count** (`computeOutOfDate`/`annotateOutOfDate` → `libraryView.OutOfDate`, counts matched models whose *cached* detail has `buildVersionBreakdown().UpdateAvailable`; NO fetch, understates cold-cache; only on terminal/page render, not the scan poll); (3) **Scan/Rescan controls → `<dialog>`** once results exist (inline before first scan; `filesTabBody(...,hasResults)`); (4) dashboard suggestions get a **lazy per-card version badge** (`GET /models/{id}/version-status`, cache-first, one fetch on miss) + CSS hover popover (newest-local → latest-available + publish date). Audit no-🔴 (version-status egress/SSRF/XSS/panic + sort whitelist + cache-only count all sound); the one 🟡 — a racy `<dialog>` `onsubmit` d.close() that could interfere with htmx submit — **removed** (success HX-Redirect reloads → dialog gone deterministically). Live-verified vs the real instance (226 matched / 200 cached): dropdown threads `?sort=Newest&period=Week`, pills show **80 out-of-date** cache-only, scan dialog present, a real badge popover ("SnatchKiss High v11 · Published 2026-06-02"). **Note:** matched-model cards on the library page still show "Model #{id}" not the resolved name (pre-existing, out of this batch's scope — candidate follow-up).

## Cross-repo (user ships CivitAI code)
- `civitai/civitai` (prod) + `civitai/cli` (private SDK) work landed in the batch-by-hash era (see git). The SDK already exposes `SearchModels`/`SearchImages`/`SearchCreators`/`GetModel`/`GetModelVersion(sByHashes)` — no SDK change was needed for any v0.1.2x feature. `civitai/cli` branch protection has a `pins-vs-published` required check the USER resolves.

## The verify/ship loop that held up
subagent (COMMIT IN SMALL COMPILABLE INCREMENTS) → **real gate with GOPRIVATE** (`go build/vet/test`, +`-race` on web/store) — this is the arbiter over BOTH the agent's "green" claim AND the editor's stale diagnostics → **`/audit-pr`** on every web/endpoint PR (it surfaced a real 🟡 on nearly all of them; fold them in before merge) → **live-verify by HTTP-level request reproduction** against the `:8972` dogfood binary (no real browser here — see CLAUDE.md; use throwaway temp-DB seeders for mutating actions) → push+tag+release (standing auth) → **pull tarball, checksum, run `--version`** (deployed ≠ verified).

## Gotchas re-confirmed this session
- **Signature-changing subagents ALWAYS trip a stale `<new-diagnostics>` cascade** — `WrongArgCount` on shared page-builders, `stubReader does not implement Reader (missing GetModelVersionsByHashes)`, `X redeclared`, phantom `zz_dbg_test.go`, `go.mod needs tidy`. `go vet` (compiles tests) + `go test` with GOPRIVATE refutes them; `go mod tidy -diff` empty refutes the tidy warning. Do NOT "fix" these before running the real gate.
- **No real browser** (MCP Playwright broken; chromium not installed). HTTP-level reproduction verifies the server response, not the DOM/JS — state that caveat when reporting.

## Open threads / next steps
1. **ComfyUI Slice C — remote CivitAI Comfy Cloud** (last slice; design in `claudedocs/COMFYUI-INTEGRATION-DESIGN.md`). Orchestration API `POST /v2/consumer/workflows`, `whatif=true` Buzz cost preview, poll-by-token; `InvokeCustomComfyStepTemplate` submits a raw comfy graph but you must **declare every resource** (URNs). **Verify the exact CustomComfy request-body field names against the live orchestration schema/playground FIRST** — that's the one genuinely uncertain piece. Auth = existing CivitAI `token` (Bearer). Make the civitai.com egress obvious (like `match_remote`). A run-backend interface (local | cloud) can be introduced so the existing run job/gallery is reused. Highest uncertainty → smallest first slice (whatif cost preview + submit + poll + results).
2. **Slice B converter edge cases to check when they bite** (not blocking): object-form `widgets_values` (rgthree Power Lora Loader), complex/chained bypass passthrough, group nodes, primitive-node widget promotion — the converter emits a warning and the run aborts rather than mis-submitting, so they're safe-but-unrunnable until handled. Also: run aborts on ANY conversion warning (a workflow needing an uninstalled custom node won't submit); one global active run (not per-workflow); poll-based progress (no live WS per-node bar).
3. **F2 — loopback-gate the outbound-proxy GETs** (`/models`, `/search`, `/creators`, `/subscribe/search`, `/models/{id}/community`, `/models/{id}/title`, popular default). Only matters on a non-loopback `--addr`; SSRF-shaped but low-severity (fixed civitai.com host, integer ids). Decide gate-all vs accept-and-document. Lower priority than ComfyUI.
4. **Deferred/minor:** `compactCount` `999_999 → "1000K"` (intended edge); the recurring `h.StyleAttr is deprecated` warnings (cosmetic); CI Node-20→24 actions-bump warning on releases; **`TestScanStatusRaceSafe` (`scan_web_test.go`) is a pre-existing rare flake** — fails ~1-in-3 ONLY under full-suite `-race` load (assertion, never a `DATA RACE`; passes 5/5 isolated). Not Slice-B-related; harden its timing later.

## Cleanup pending
- **Dogfood server is running unattended** — v0.1.33 build on `:8972` against the real DB (`~/.config/civitai-manager/civitai-manager.db`); schema still `0009`. The user's real DB now holds their **61 scanned workflows** (a prior diagnostic scan's 288 rows incl. 226 template/example noise were cleared, then re-scanned clean with the v0.1.31 fix). **Runnable**: Library → **Workflows** → **Run** executes on the local ComfyUI at `:8188`.
- **Side effect of live-verify:** a test image `OneWordChallenge_00001_.png` was generated into the user's ComfyUI `output/` dir (a real render during end-to-end verification). Harmless; delete if unwanted.
- Session **scratchpad** has a `dogfood/cm` binary — clear when convenient.
